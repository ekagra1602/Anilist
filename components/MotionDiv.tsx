"use client";
import {motion} from "framer-motion";

export const MotionDiv = motion.div;
//We create this component so that we can make our anime card use server side rendering while also using framer motion